[package]
name = "nato-flutter"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["staticlib", "cdylib"]
# staticlib = iOS/macOS static linking; cdylib = Android .so

[dependencies]
nato = { path = "../nato" }
flutter_rust_bridge = "=2.12.0"
